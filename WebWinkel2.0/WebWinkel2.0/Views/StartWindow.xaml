<Window x:Class="WebWinkel2._0.StartWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Hallo klant" Height="200" Width="300"
         DataContext="{Binding Manager, Source={StaticResource Locator}}">



    <Grid>
        <StackPanel Margin="0,0,0,20">
            <!--<Button Command="{Binding ShowAfdelingWindowCommand}" Height="50">Zie Afdelingen</Button>-->
            <!--<Button Command="{Binding ShowMerkWindowCommand}" Height="50">Zie Merken</Button>-->
            <!--<Button Command="{Binding ShowMerkWindowCommand}" Height="50">Zie Merken</Button>-->
            <Button Command="{Binding ShowProductWindowCommand}" Height="50" Content="Zie Producten"/>
            <Button Command="{Binding ShowProductWindowCommand}" Height="50" Content="Zie Recepten TODO"/>
            <!--<Button Command="{Binding ShowEindproductWindowCommand}" Height="50">Zie Eindproducten</Button>-->
        </StackPanel>
    </Grid>
</Window>
