<Window x:Class="webwinkelAH.View.Home"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Home" Height="300" Width="300">
    <StackPanel>
        <ToolBar Background="CornflowerBlue" ToolBarTray.IsLocked="True" Height="30">
            <Button Margin="0 0 5 0" BorderBrush="White">Login</Button>
            <Button Margin="0 0 5 0" BorderBrush="White">About</Button>
            <Label>€0,00</Label>
        </ToolBar>

        <Grid Height="200">
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>

            <Button>Categorieen</Button>
            <Button Grid.Column="1">Boodschappenlijst</Button>
            <Button Grid.Row="1">Recepten</Button>
        </Grid>
    </StackPanel>
</Window>
